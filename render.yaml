services:
  - type: web
    name: holly-expense-tracker
    env: python
    buildCommand: pip install -r requirements.txt && python database.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
